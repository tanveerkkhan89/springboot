replicaCount: 2

image:
  repository: khatanve/my-image
  tag: ""
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  targetPort: 8080

imagePullSecrets:
  - name: my-docker-registry-secret

resources: {}
